# ContextGC website

A dependency-free static landing and installation guide for ContextGC.

## Run locally

From the repository root:

```bash
python3 -m http.server 8000 --directory website
```

Open <http://localhost:8000>.

The site is plain HTML, CSS, and JavaScript. It can be hosted directly from GitHub Pages, any static host, or a local file server.
